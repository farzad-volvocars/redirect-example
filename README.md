This simple apps collection demonstrate how we may have a series of backend nodes that redirect a single request. The code is in Go.

To start, for each project run

```
make
```

start from http://localhost:3000
